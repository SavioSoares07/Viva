<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.viva">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Hidrata">

        <activity
            android:name="com.example.viva.TelaAguaActivity"
            android:exported="false" />

        <activity
            android:name="com.example.viva.TelaPassosActivity"
            android:exported="false" />

        <activity
            android:name="com.example.viva.TelaSonoActivity"
            android:exported="false" />

        <activity
            android:name="com.example.viva.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
